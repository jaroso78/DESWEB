{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Horario</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('horario_index') }}">Regresar a la lista</a>
        </li>
    </ul>
{% endblock %}
