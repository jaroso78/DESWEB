{% extends 'base.html.twig' %}

{% block body %}
    <h1>Alumnos</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alumno.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ alumno.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ alumno.apellidos }}</td>
            </tr>
            <tr>
                <th>Numéro de Telefono</th>
                <td>{{ alumno.numTelefono }}</td>
            </tr>
            <tr>
                <th>Estudios Cursados</th>
                <td>{{ alumno.estudiosCursados }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('alumnos_index') }}">Regresar a la lista</a>
        </li>
        <li>
            <a href="{{ path('alumnos_edit', { 'id': alumno.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Borrar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
